.. The contents of this file are included in multiple topics.
.. This file should not be changed in a way that hinders its ability to appear in multiple documentation sets.

The |lwrp gunicorn install| lightweight resource is used to install the latest version of |gunicorn| to a targeted virtual environment using |python pip|. This action overrides ``node["gunicorn"]["virtualenv"]``.
